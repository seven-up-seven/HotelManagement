<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.text.*?>
<?import java.time.*?>

<BorderPane xmlns="http://javafx.com/javafx/23.0.1"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.example.frontendquanlikhachsan.controllers.HomeController"
            prefWidth="800" prefHeight="500">

    <!-- Top: Tiêu đề -->
    <top>
        <Label text="Trang chủ quản lý khách sạn"
               style="-fx-font-size: 20px; -fx-font-weight: bold;"
               BorderPane.alignment="CENTER" />
    </top>

    <!-- Left: Xin chào + vai trò -->
    <left>
        <VBox spacing="10" padding="10">
            <Label fx:id="helloLabel" text="Xin chào, [Vai trò] [Tên]" />
        </VBox>
    </left>

    <!-- Right: Đồng hồ thời gian thực -->
    <right>
        <VBox spacing="10" padding="10">
            <Label text="Thời gian hiện tại:" />
            <Label fx:id="clockLabel" text="00:00:00" style="-fx-font-size: 14px;"/>
        </VBox>
    </right>

    <!-- Center: Form thông tin người dùng -->
    <center>
        <GridPane fx:id="infoForm" hgap="10" vgap="10" padding="20">
            <Label text="Mã nhân viên:" GridPane.rowIndex="0" GridPane.columnIndex="0"/>
            <Label fx:id="staffIdLabel" GridPane.rowIndex="0" GridPane.columnIndex="1"/>

            <Label text="Tên nhân viên:" GridPane.rowIndex="1" GridPane.columnIndex="0"/>
            <Label fx:id="staffNameLabel" GridPane.rowIndex="1" GridPane.columnIndex="1"/>

            <Label text="Email:" GridPane.rowIndex="2" GridPane.columnIndex="0"/>
            <Label fx:id="staffEmailLabel" GridPane.rowIndex="2" GridPane.columnIndex="1"/>

            <Label text="Chức vụ:" GridPane.rowIndex="3" GridPane.columnIndex="0"/>
            <Label fx:id="staffRoleLabel" GridPane.rowIndex="3" GridPane.columnIndex="1"/>
        </GridPane>
    </center>
</BorderPane>
